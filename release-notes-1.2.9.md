# Sky Vanguard 1.2.9

- Fix packaged preload script so the updater bridge loads in installed builds
- Restore startup update check timing so checks run even if the UI is slow to mount
- Raise update dialog z-index so it stays visible above the menu
